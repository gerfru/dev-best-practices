---
description: Performance engineering workflow based on MIT 6.172 (Bentley Rules) and Brendan Gregg USE Method — systematic bottleneck analysis with flamegraphs, profiling, and verified before/after benchmarks.
argument-hint: "[optional: context, e.g. 'Python API p99 > 800ms' or 'Node.js memory leak in production' or 'DB queries too slow']"
---

Analyze the performance problem with the structured performance engineering workflow.
Follow exactly the workflow definition in `${CLAUDE_PLUGIN_ROOT}/skills/tool-perf/SKILL.md`.

Context: $ARGUMENTS
